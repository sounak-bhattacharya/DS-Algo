void explainUSet(){
    unordered set<int> st;
    //lower bound and upper bound functions does not work , rest all functions are same as above, it does not stores in any perticular order it has a better complexity than set in most cases, except some when collision happens.
}